[project]
name = "security-dashboard-backend"
version = "0.1.0"
license = "Apache-2.0"

dependencies = [
  "python-dotenv",
  "google-auth-oauthlib",
  "google-api-python-client",
  "google-cloud-pubsub",
]
